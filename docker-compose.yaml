services:
  backend:
    build: ./Backend
    container_name: backend
    ports:
      - "5000:5000"

  frontend:
    build: ./Frontend
    container_name: frontend
    ports:
      - "8501:8501"
    depends_on:
      - backend
